// This component is no longer used — game pages are handled by src/app/[game]/page.tsx
// Keeping the file to avoid breaking any lingering imports during transition.
export function CatalogPage() {
  return null
}
